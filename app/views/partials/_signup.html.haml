#signup_blurb
  Signup to save your shows:
  = form_for :user, :url => {:controller => 'users', :action => 'create'} do |f|
    = f.text_field "email", { placeholder: "E-mail" }
    %br
    = f.password_field "password", { placeholder: "Password" }
    %br
    = f.submit "Sign up!", id: "signup_button"